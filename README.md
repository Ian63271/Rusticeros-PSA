#Requisitos de software.

-Rust
-Wireguard
-k3s
-docker
-Hyper-V con ISO de ubuntu server


#Instrucciones para levantar la VPN
1. Instalar WireGuard
sudo apt update
sudo apt install wireguard

#Instrucciones para desplegar contenedores (k3s).

#Instrucciones para compilar y ejecutar el sistema distribuido en Rust.

- Instrucciones para compilar y ejecutar el sistema distribuido en Rust.
ejecutar en la raiz cargo build y run